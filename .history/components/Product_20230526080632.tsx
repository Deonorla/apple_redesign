import { urlFor } from "@/sanity";
import React from "react";
interface Props {
  product: Product;
}
const Product = ({ product }: Props) => {
  return (
    <div>
      <div></div>
    </div>
  );
};

export default Product;
